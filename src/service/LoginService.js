export default class LoginService{
    static logar(codigoUsuario, senha){
        if(this.email == "terenciani@outlook.com" && this.senha=='123'){
            this.mensagem = "Login ok"
            this.cor = "success"
            this.alert = true
        }else{
           return false
        }
    }
    static recuperarSenha(email)
}